#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include<time.h>


int main()

{
//	int* p;//局部变量不初始化，里面放随机值
//	*p = 20;//(未定义野指针）
	/*int a[10] = { 0 };
	int i = 0;
	int* p = a;
	for (i = 0; i <= 12; i++)
	{
		*p = i;
		p++;
	}
	*///(越界野指针)
	/*int* text()
	{
		int a = 10;
		return &a;
	}
	{
		int* p = text();
		printf("%d\n", *p);

	}
	*///()
	int arr[10] = { 1,2,3,4,5,6,7,8,9,0 };
	int i = 0;
	int sz = sizeof(arr) / sizeof(arr[0]);
	for (i = 1; i < sz; i++)
	{
		printf("%d", *p);
		p=p+1;
	}
	return 0;
}
二
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include<time.h>

int my_strlen(char*str)

int main()

{
	
	char arr[] = "bit";
	int len = my_strlen(arr);
	printf("%d\n", len);
	return 0;
}


